(jbuild_version 1)

(executable
 ((name query)
  (modules (query))
  (flags (-ppx ppx_lwt))
  (libraries (caqti caqti-lwt))))

(install
 ((section bin)
  (package caqti-lwt)
  (files ((query.exe as caqtiq)))))
